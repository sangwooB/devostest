---
- hosts: webserver
  gather_facts: False
  become: true
  become_method: sudo
  
  tasks:    
    - name: Install Nginx
      yum:
        name: nginx
        state: present
    
    - name: Start Nginx
      service:
        name: nginx
        state: started